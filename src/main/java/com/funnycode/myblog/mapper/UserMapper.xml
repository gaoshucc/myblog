<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.funnycode.myblog.mapper.UserMapper">

    <select id="userexists" parameterType="java.lang.String" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM `user` WHERE username = #{username}
    </select>

    <select id="nicknameexists" parameterType="java.lang.String" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM `user` WHERE nickname = #{username}
    </select>

    <insert id="regist" parameterType="com.funnycode.myblog.pojo.User">
        INSERT INTO `user`(`user_id`,`status`,`last_logintime`,`username`,`password`,`nickname`,`role`,`profile_path`,`experience`,`gender`,`motto`,`position_id`,`regtime`) VALUES (#{userId},#{status},#{lastLogintime},#{username},#{password},#{nickname},#{role},#{profilePath},#{experience},#{gender},#{motto},#{positionId},#{regtime})
    </insert>

    <select id="login" parameterType="com.funnycode.myblog.pojo.User" resultType="com.funnycode.myblog.pojo.User">
        SELECT * FROM `user` WHERE username = #{username} and password = #{password}
    </select>

    <!--
    <resultMap id="blogger" type="com.funnycode.myblog.pojo.User">
        <id property="userId" column="user_id"></id>
        <result property="lastLogintime" column="last_logintime"></result>
        <result property="profilePath" column="profile_path"></result>
        <result property="positionId" column="position_id"></result>
        <collection property="notes" column="user_id" select="com.funnycode.myblog.mapper.NoteMapper.findNotesByBloggerId"></collection>
    </resultMap>
    <select id="getBloggerByUserId" parameterType="java.lang.String" resultMap="blogger">
        SELECT * FROM `user` WHERE user_id = #{userId}
    </select>
    -->

    <select id="getUserByUserId" parameterType="java.lang.String" resultType="com.funnycode.myblog.pojo.User">
        SELECT * FROM `user` WHERE user_id = #{userId}
    </select>

    <select id="getUserByUsername" parameterType="java.lang.String" resultType="com.funnycode.myblog.pojo.User">
        SELECT * FROM `user` WHERE username = #{username}
    </select>

    <select id="getPasswordByUsername" parameterType="java.lang.String" resultType="java.lang.String">
        SELECT password FROM `user` WHERE username = #{username}
    </select>

    <select id="getRoleByUsername" parameterType="java.lang.String" resultType="java.lang.String">
        SELECT role FROM `user` WHERE username = #{username}
    </select>

    <resultMap id="Blogger" type="com.funnycode.myblog.pojo.User">
        <id property="userId" column="user_id"></id>
        <result property="nickname" column="nickname"></result>
    </resultMap>
    <select id="findBloggerByUserId" parameterType="java.lang.String" resultMap="Blogger">
        SELECT user_id,nickname FROM `user` WHERE user_id = #{userId}
    </select>

    <resultMap id="BloggerDetail" type="com.funnycode.myblog.pojo.User">
        <id property="userId" column="user_id"></id>
        <result property="nickname" column="nickname"></result>
        <result property="profilePath" column="profile_path"></result>
        <result property="gender" column="gender"></result>
        <result property="experience" column="experience"></result>
        <result property="positionId" column="position_id"></result>
    </resultMap>
    <select id="findBloggerDetailByUserId" parameterType="java.lang.String" resultMap="BloggerDetail">
        SELECT user_id,nickname,profile_path,gender,experience,position_id FROM `user` WHERE user_id = #{userId}
    </select>

    <update id="updateUserLastLogintimeByUsername" parameterType="com.funnycode.myblog.pojo.User">
        UPDATE `user`
        <trim prefix="set" suffixOverrides=",">
            <if test="lastLogintime != null">last_logintime = #{lastLogintime},</if>
        </trim>
        WHERE username = #{username}
    </update>

    <insert id="saveNote" parameterType="com.funnycode.myblog.pojo.Note">
        INSERT INTO `note`(`note_id`,`note_title`,`note_type`,`create_time`,`status`,`blogger`,`note_html`)
        VALUES (#{noteId},#{noteTitle},#{noteType.typeId},#{createTime},#{status},#{blogger.userId},#{noteHtml})
    </insert>

    <update id="updateUserExperienceByUserId" parameterType="java.lang.String">
        UPDATE `user` SET experience = experience+100 WHERE user_id = #{userId}
    </update>
</mapper>